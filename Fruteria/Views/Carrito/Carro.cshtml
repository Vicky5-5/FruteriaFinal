@model IEnumerable<Fruteria_vgarcia.ViewModels.CarritoViewModel>

@{
    ViewBag.Title = "Carro";
}

<h2>Carro</h2>

<table class="table">
    <thead>
        <tr>

            <th>
                Nombre del Producto
            </th>
            <th>
                Precio
            </th>

            <th>
                Descripción
            </th>

            <th>
                Imagen
            </th>
            <th>
                Cantidad
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @*@foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.oProductos.NombreProducto)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.oProductos.Precio)
                    </td>

                    <td>
                        @Html.DisplayFor(modelItem => item.oProductos.Descripcion)
                    </td>

                    <td>
                        <img src="@item.oProductos.ImagenProductos" width="100" height="100" alt="Imagen del producto" />
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CantidadCompra)
                    </td>
                    <td>
                        @Html.ActionLink("Añadir Más", "AddToCart", "Carrito")
                    </td>
                </tr>
            }*@

    </tbody>

</table>
<div class="row" id="fila">
    <div class="form-group col-md-6">
        <div class="col-md-offset-2 col-md-10">
            <input id="boton" type="submit" value="Comprar" class="btn btn-default" /> @*Hacer que se guarde en pedido*@
        </div>
    </div>
    <br />

    <div class="col-md-6">
        @Html.ActionLink("Volver", "Index")
    </div>
</div>

<style>
    #boton {
        border-radius: 5px;
        background-color: black;
        color: white;
        width: 10vw;
    }
</style>